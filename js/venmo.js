const main = async () => {
    const txs = []
    while (true) {
        const response = await fetch()
        const data = await response.json()
        if (!data.pagination.next) break
        txs.push()
    }
}